export const metadata = {
  title: "awarm.space: shelf",
  description: "a list of things i like",
};

export default function ShelfLayout(props: { children: React.ReactNode }) {
  return <>{props.children}</>;
}
